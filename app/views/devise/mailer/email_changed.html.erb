<div class="container">
  <div class="row">
    <div class="text-center" >
      <p>こんにちは<%= @name %>!</p>
      <br>
      <% if @resource.try(:unconfirmed_email?) %>
        <p>メールがに変更されることを通知するためにご連絡いたします<%= @resource.unconfirmed_email %>.</p>
      <% else %>
        <p>メールアドレスが変更されたことをお知らせするためにご連絡いたします <%= @resource.email %>.</p>
      <% end %>
    </div>
  </div>
</div>
