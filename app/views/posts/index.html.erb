<div class="container">
  <div class="row">
<h1>投稿一覧</h1>
<% @posts.each do |post| %>
  <p>タイトル</p>
  <%= link_to post.title, posts_path(post.id) %>
  <%= attachment_image_tag post, :image, :fill, 100, 100, format: 'jpeg' %>
  <%= post.body %>
<% end %>
</div>
</div>